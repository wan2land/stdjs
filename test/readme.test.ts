
require("jest") // tslint:disable-line

describe("readmd", () => {
  it("hello world", () => {
    expect(true).toBeTruthy()
  })
})
    // console.log(await connection.query("insert into actor(first_name, last_name) value('Cris', 'X')"))
    // console.log(await connection.query("update actor set first_name = 'Cris2' where actor_id = 281"))
    // console.log(await connection.query("delete from actor where actor_id = 282"))
    // console.log(await connection.select("select * from actor order by actor_id desc limit 10"))

