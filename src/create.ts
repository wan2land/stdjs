// import { RelaterConfig } from "./interfaces/common"
// import { Relater } from "./relater/relater"


// export function create(config: RelaterConfig): Relater {
//   // const connectionKeys = Object.keys(connections)
//   // if (connectionKeys.length === 0) {
//   //   throw new Error("at least 1 connection is required.")
//   // }

//   // for (const name of connectionKeys) {
//   //   this.connections[name] = create(connections[name])
//   // }
//   // this.defaultConnection = connections[defaultConnection] ? defaultConnection : connectionKeys[0]

//   // return new Relater(config)
// }
